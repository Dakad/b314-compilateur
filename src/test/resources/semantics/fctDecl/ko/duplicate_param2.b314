declare and retain
  b as boolean;

  f as function(i as integer): boolean
  do
    set b to true
    return false
  done

  fun as function(bol as boolean, bol as integer): integer
  do
    set b to false
    return true
  done

  by default do
    skip
  done
