declare and retain
  b as boolean;

  f as function(f as integer): boolean
  do
    set b to true
    return false
  done

  fun as function(it as integer, fun as boolean): void
  do
    set b to false
    return void
  done

  by default do
    skip
  done
