declare and retain
  b as boolean;

  f as function(i as integer): boolean
  do
    set b to true
    return false
  done


  fun as function(i as integer, s as square): void
  do
    set b to true
    return void
  done

  by default do
    skip
  done
