/* My comments */
declare and retain
	b1 as boolean;
	b2 as boolean;
    i1 as integer;
    i2 as integer;
    s1 as square;
    s2 as square;

    fct2 as function(x as integer, y as boolean):integer
    	do
    		skip
    		return i1
    	done

    fct3 as function():boolean
    	do
    		skip
    		return b1
    	done

    fct4 as function():square
    	do
    		skip
    		return s1
    	done

    fct5 as function(x as integer, y as boolean, z as square):integer
    	do
    		skip
    		return i2
    	done

when your turn

	when fct3() do
		skip
	done

	when fct5(1, false) = i1 do
		skip
	done

	when fct5(v2[1], b1 and true, s2) = 42 do
		skip
	done

	by default do
		skip
	done