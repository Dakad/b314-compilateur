/*my commentaire */

declare and retain
  x as integer;
  res as square[43,43];

  f as function (t as boolean) : void
  do
    set t to true
    return void
  done

  by default do
    compute f(x = 12)

  done