declare and retain
    x as integer;
    s as square;
    z as boolean;
    f as function ( x as boolean, y as boolean): boolean
        do
            if x=y then set y to true done
            return x
        done
    f2 as function (a as boolean, b as boolean): boolean
        do
            if a=b then set b to false done
            return z
        done
when your turn
    by default
        do
            compute not x < 5 and f(z=true, true)=true and f(true, false)=f2(true, true) or not f2(true, false)
            compute x=2 and ennemi is north
            compute not graal is south or ennemi is north and x > 42
            compute nearby [2,2] = ammo and s = map and not x < 5
        done
