declare and retain
    import inputFile.wld
    t as boolean;
    x as square;

    f as function( ): boolean
    do
        set t to true
       return t
    done

when your turn

	when x = ammo do
		skip
	done

	by default do
		next move south
	done