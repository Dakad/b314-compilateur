/* My commentaire */
declare and retain
	b1 as boolean;
	b2 as boolean;
    i1 as integer;
    i2 as integer;
    v1 as boolean[5, 7];
    v2 as integer[42];

    fct1 as function():boolean
    	do
    		set fct1 to true
    		return fct1;
    	done

when your turn

	when not b1 do
		set b1 to true
		next move north
	done

	when b1
	declare local
		b11 as integer;
		i11 as boolean;
		loc3 as square;
		loc4 as integer[5];
	do
		set b1 to 34
		set i1 to false
		set i1 to b1 < 42 and i1
		next move east
	done

	by default do
		next do nothing
	done