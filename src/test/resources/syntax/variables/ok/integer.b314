declare and retain
    import inputFile.wld
    t as integer;
    res as integer[4];
    res2 as integer[4,45];

    f as function( ): boolean
    do
        set t to 12
        return false
    done

when your turn

	when nearby[1,2] = ammo do
		skip
	done

	by default do
		skip
	done