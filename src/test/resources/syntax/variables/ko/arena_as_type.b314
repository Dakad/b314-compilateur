declare and retain
    import inputFile.wld
    t as arena;
    r as arena[3];
    res as arena[43,43];

    f as function( ): boolean
    do
        set t to true
        return res;
    done

when your turn

	when x = ammo do
		skip
	done

	by default do
		skip 
	done